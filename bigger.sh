#!/bin/bash

if [$1 -gt &2 ] && [$1 -gt $3 ];
then
		echo "$1 is biiger number "
		
elif  [$1 -gt &2 ] && [$1 -gt $3 ];
then
	echo " $2 is the bigger number "
	
else
   echo " $3 is the bigger number "
fi 

